队列
分布式队列是一种常见的数据结构。想要通过zookeeper实现分布式队列，首先指派一个znode来保存
队列，也就是queue node。分布式的客户端通过调用create()方法(路径“queue-”,属性设置为顺序和临时)
来往队列中添加东西。因为顺序属性的作用，新路径将会以这种形式path-to-queue-node/queue-X,其中
X是单个递增的数字。一个客户端如果想要从队列中消费，它需要调用getChildren()方法，并且在
queue node 上设置监视，然后开始处理数字最小的节点。客户端不需要发出另一个getChildren，直到它
用光了第一次getChilren()获得的列表。如果queue node中已经没有子节点了，那么消费者将等待监控的触发，然后
重新检查队列。
提示：队列实现已经包含在ZK菜谱中,发布在zookeeper-recipes/zookeeper-recipes-queue文件夹中

优先级队列
为了实现优先级队列，你只需要在普通队列菜谱的基础上修改2个地方。首先队列节点的路径应该改为“queue-YY”
其中YY代表着优先级，数字越小表示越高的优先级(就像UNIX那样)。第二，当从队列中消费时，客户端总是使用最新的
子节点列表，这意味着客户端将在监控触发时丢弃上一次获得的子节点列表。
